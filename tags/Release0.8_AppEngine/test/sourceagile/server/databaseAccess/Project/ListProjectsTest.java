package sourceagile.server.databaseAccess.Project;

import junit.framework.TestCase;

/**
 * Test Class: ListProjects. List all the projects added in the system.
 * 
 */
public class ListProjectsTest extends TestCase {

	public void testList() {

		// PersistenceManager persistenceManager;

		// Project> listTested = ListProjects.list(persistenceManager);

		// assertEquals(listTested, null);
	}

}
